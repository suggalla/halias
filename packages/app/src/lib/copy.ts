// Copy that appears in more than one place, kept in one.
//
// Two windows offer the same thing — let another account broadcast this and take a fee for
// it — and they had drifted into describing it differently: one as a relayer feature, the
// other as "I have no ETH for gas", each with its own paragraph of reassurance. Same
// mechanism, so the same words, and short enough to read.

export const RELAY_LABEL = 'Submit with another account';

export const RELAY_HINT =
	'A relayer, or another wallet of your own. Whoever sends it pays the gas and takes the fee, ' +
	'so you need no ETH.';
