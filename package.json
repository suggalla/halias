{
  "name": "halias",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "halias": "node packages/sdk/dist/cli.js"
  }
}
